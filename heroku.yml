setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: my-example-bucket
    DB_HOST: localhost
    DB_PORT: 27017
    DB_NAME: ogrenciden-server
    APP_PORT: 3000
    PASSWORD_HASH: 27201bfef43467924b5853dd678bcbfc3f278e7b0grenc!denp0jes1
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
  config:
    RAILS_ENV: development
    FOO: bar
release:
  command:
    - ./deployment-tasks.sh
  image: worker
run:
  web: bundle exec puma -C config/puma.rb
  worker: python myworker.py
  asset-syncer:
    command:
      - python asset-syncer.py
    image: worker
